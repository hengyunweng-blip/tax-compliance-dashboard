# Gate 5 基线修复重跑报告

复核日期：2026-08-29（Australia/Melbourne）  
当前提交：`3fa604dbdec25b4f365d94ec89210403827359da`  
本报告只覆盖已授权的三项修复；没有创建 `gate-5`，也没有修改或删除 `docs/evidence/gate0` 至 `docs/evidence/gate5` 下的既有证据。

## 1. 本轮范围与提交

- 年度底稿收入、运营费用、资本采购按 `amount_cents - gst_cents` 采用 GST 不含税口径；`PRIVATE` 排除，`GST_FREE_INCOME`、`INPUT_TAXED`、`NO_GST` 的 GST 为 0 时全额计入。
- Div 7A 以期初余额加当年利息减当年已记录还款逐年推进，`term_years` 是原始期限，`remainingTermYears` 是当前评估年度的剩余期限；期满后状态为 `expired`。
- 状态流转到 `lodged` 或 `paid` 时，均要求用户传入并保存实际日期；不使用当天日期。

相关提交：

- `a6681bafe351bd68d642e1e8e1b3e26ba8a45ec8`：年度 GST 口径与 Div 7A 余额修复。
- `3fa604dbdec25b4f365d94ec89210403827359da`：补齐通用状态接口的用户递交日期持久化。

## 2. 年度底稿：四季演练重跑

在独立复制的阶段 3 证据数据库上使用当前代码重读四份已完成 BAS，并重新计算当前年度底稿。数据库路径为临时目录：
`/var/folders/vn/n8trtj0x74j5ggwrwlx1n6pm0000gn/T/tax-gate5-stage3-rerun.XXXXXX.ZhZ6L5yzoQ/stage3.db`。

以下均为整数分；`G1` 是含 GST 销售总额，年度收入/费用/资本采购是 GST 不含税金额：

| 期间 | G1 | 1A | 1B | G10 | G11 | 5A | 5B | statementTotal | 状态 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| FY2026–27 Q1 | 190,000 | 10,000 | 25,000 | 220,000 | 55,000 | 2,500 | 0 | -12,500 | paid |
| FY2026–27 Q2 | 320,000 | 20,000 | 32,500 | 275,000 | 80,000 | 3,000 | 500 | -10,000 | paid |
| FY2026–27 Q3 | 450,000 | 30,000 | 40,000 | 330,000 | 105,000 | 0 | 0 | -10,000 | paid |
| FY2026–27 Q4 | 690,000 | 50,000 | 47,500 | 385,000 | 130,000 | 5,000 | 1,000 | 6,500 | paid |
| **合计** | **1,650,000** | **110,000** | **145,000** | **1,210,000** | **370,000** | **10,500** | **1,500** | — | — |

当前代码计算出的年度底稿：

| 项目 | 实际值（分） | 显示值 |
|---|---:|---:|
| 收入（不含 GST） | 1,540,000 | $15,400.00 |
| 运营费用（不含 GST） | -533,000 | -$5,330.00 |
| 资本采购（不含 GST） | 1,100,000 | $11,000.00 |
| 账面净利润（不含 GST） | 1,007,000 | $10,070.00 |
| 来源交易 | 25 笔 | — |

新的交叉口径为：

```text
四份 BAS 的 G1 合计 - 1A 合计
= 1,650,000 - 110,000
= 1,540,000 分
= 年度底稿 incomeCents
```

这修正了原先把含 GST 的 G1 直接等同于年度税表收入的错误。年度页面、API 的 `amountBasis: "GST-exclusive"` 和页面字段均明确写出“不含 GST”。对应视觉证据仍在既有的 [`annual-net-basis.jpg`](./annual-net-basis.jpg)，本轮没有覆盖该文件。

针对单笔 `$1,100` GST_INCOME 的单元测试实际断言：BAS `G1 = 110,000` 分，年度 `incomeCents = 100,000` 分，见 [`tests/unit/annual-worksheets.test.ts`](../../../tests/unit/annual-worksheets.test.ts) 的“keeps BAS G1 GST-inclusive...”用例。

## 3. Div 7A 余额逐年推进

### 3.1 官方基准

官方基准仍从 [`tests/fixtures/div7a/ato-baseline.json`](../../../tests/fixtures/div7a/ato-baseline.json) 读取，不由实现代码计算期望值。输入为 `$100,000 / 5.30% / 7 年 / FY2017–18`，fixture 中保留 ATO 官方计算器 URL 与取数日期 `2026-08-27`，官方输出为 `1,747,034` 分。测试代码见 [`tests/unit/div7a.test.ts`](../../../tests/unit/div7a.test.ts)，该基准本轮未改动。

### 3.2 逐年余额与期限

单元测试覆盖 2017-05-15 发放、原始期限 7 年、本金 10,000,000 分、手动基准利率 5.30% 的滚动：

| 所得年度 | 期初余额 | 本年利息 | 最低还款 | 实际还款 | 期末余额 | 状态 |
|---|---:|---:|---:|---:|---:|---|
| FY2016–17 | 10,000,000 | 0 | 0 | 0 | 10,000,000 | origination |
| FY2017–18 | 10,000,000 | 530,000 | 1,747,034 | 1,747,034 | 8,782,966 | active |
| FY2018–19 | 8,782,966 | 465,497 | 1,747,035 | 1,747,035 | 7,501,428 | active |
| FY2019–20 | 7,501,428 | 397,576 | 1,747,034 | 1,747,034 | 6,151,970 | active |
| FY2020–21 | 6,151,970 | 326,054 | 1,747,035 | 1,747,035 | 4,730,989 | active |
| FY2023–24 | 1,659,102 | 87,932 | 0 | 0 | 1,747,034 | expired |

该序列证明后续年度使用上一年度余额，而不是每年把本金当作期初余额。对同一贷款“一分未还”的 FY2019–20 结果为期初 `10,530,000`、最低还款 `2,094,540`、期末 `11,088,090`，与每年按最低额还款的第三年结果不同；测试同时断言余额和最低还款额均不同。FY2023–24 及以后为已到期，不继续产生最低还款。

页面展示每年期初余额、利息、实际还款和期末余额；既有 [`div7a-yearly-balances.jpg`](./div7a-yearly-balances.jpg) 与 [`div7a-expired.jpg`](./div7a-expired.jpg) 未被覆盖。

## 4. `lodged_at` 与 `paid_at`

通用 `PATCH /api/obligations/[id]/transition` 现在要求：

- `draft_ready -> lodged` 必须传入合法 `lodgedAt`，并保存该值；缺失时返回 400，状态保持 `draft_ready`、字段保持 NULL。
- `lodged -> paid` 必须传入合法 `paidAt`，并保存该值；不默认当天。
- BAS 专用 lodge 路径原本已有相同的用户日期约束，本轮保留。

新增测试见 [`tests/unit/obligation-state.test.ts`](../../../tests/unit/obligation-state.test.ts) 的“requires and persists...”用例；已有 BAS 测试继续覆盖实际缴款日期。当前表单输入提示固定为 `DD/MM/YYYY`。

## 5. 干净 clone 的真实基线

干净 clone：`/var/folders/vn/n8trtj0x74j5ggwrwlx1n6pm0000gn/T/tax-gate5-clone.XXXXXX.16Tt47ucmM`。Node `v26.7.0`、npm `11.19.0`。该 clone 从当前 `main` 提交创建，先完成 `npm ci`，再执行迁移和 seed。

| 检查 | 实际结果 |
|---|---|
| `npm run db:migrate` | 退出码 0 |
| `npm run db:seed` | 退出码 0 |
| `npm test -- --run` | 29 个文件，150 个用例，150 通过，0 失败 |
| `npm test -- --run --sequence.shuffle` | 29 个文件，150 个用例，150 通过，0 失败；本次随机 seed `1787993329861` |
| `npm run lint` | 退出码 0 |
| `npm run build` | 退出码 0；Next.js 15.5.24 编译、类型检查和静态页面生成通过 |

`npm ci` 报告依赖树有 8 个漏洞（5 moderate、3 high）；本轮没有执行 `npm audit fix`，也没有扩大到依赖修复。

## 6. 结论与停止点

三项授权修复及当前验证均已完成。现有标签仍只有 `gate-0`、`gate-1`、`gate-2`、`gate-3`、`gate-4`；没有 `gate-5`。工作区除本报告新增文件外无未提交代码改动。

Gate 5 尚未由用户验收；本轮到此硬停止，不开始 Gate 6。按最新设计，Gate 5 验收后还要冻结，等待用户用真实银行数据和真实发票跑完 FY2026–27 Q1 BAS（截止 11 Nov 2026），再决定后续优先级。
